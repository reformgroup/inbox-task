<% provide :title, t('.title') %>
<% provide :subtitle, t('activerecord.models.user.name') %>

<%= render('form', user: @user, 
  save_link: submit_tag(t('buttons.save'), class: 'btn btn-primary'), 
  cancel_link: link_to(t('buttons.cancel'), users_path, class: 'btn btn-link text-muted', role: 'button')) %>
