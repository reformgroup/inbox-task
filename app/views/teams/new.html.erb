<% provide :title, t('.title') %>
<% provide :subtitle, t('activerecord.models.team.name') %>

<%= render('form', user: @team, 
  save_link: submit_tag(t('buttons.save'), class: 'btn btn-primary'), 
  cancel_link: link_to(t('buttons.cancel'), teams_path, class: 'btn btn-link text-muted', role: 'button')) %>
